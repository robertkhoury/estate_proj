<li>
  <% if micropost.unread %>
  	<span class="content"style="font-weight:bold"><%= link_to truncate(micropost.content, :length => 60), micropost %></span>
  <% else %>
  	<span class="content"><%= link_to truncate(micropost.content, :length => 60), micropost %></span>
  <% end %>
  <span class="timestamp">
    | Received <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
</li>



