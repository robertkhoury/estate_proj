<% @house.relationships.find_by(followed_id: current_user.id) %>
<%= form_for(current_user.relationships.find_by(followed_id: @house.id), html: { method: :delete }) do |f| %>
  <%= f.submit "Favorited Home (click to unfavorite)", class: "btn btn-small" %>
<% end %>